Execution mode drop down

- show Execution, when you click you see cli first, but that isn't the default.

- It should always show the real actual default and not some dummy place holder.

- it should be a little bit wider to avoid clipping

- Safety feature

  - enable deletes button to enable delete buttons across the app
  - enable changes to enable change buttons across the app
  - Doesn't change confirm request
